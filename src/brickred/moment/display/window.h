#ifndef BRICKRED_MOMENT_DISPLAY_WINDOW_H
#define BRICKRED_MOMENT_DISPLAY_WINDOW_H

#include <brickred/moment/base/class_util.h>

namespace brickred::moment::display {

class Window {
public:
    Window();
    ~Window();

private:
    BRICKRED_MOMENT_NONCOPYABLE(Window)
};

} // namespace brickred::moment::display

#endif
